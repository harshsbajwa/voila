# Airflow-specific environment variables
# Copy to .env and adjust values as needed

# Airflow home directory (set this in your shell profile or here)
AIRFLOW_HOME=/Users/harshbajwa/Code/voila/airflow

# Email configuration for failure alerts (optional)
# AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
# AIRFLOW__SMTP__SMTP_STARTTLS=True
# AIRFLOW__SMTP__SMTP_SSL=False
# AIRFLOW__SMTP__SMTP_PORT=587
# AIRFLOW__SMTP__SMTP_USER=your-email@gmail.com
# AIRFLOW__SMTP__SMTP_PASSWORD=your-app-password

# Executor configuration (default: SequentialExecutor for local development)
AIRFLOW__CORE__EXECUTOR=SequentialExecutor

# Disable example DAGs
AIRFLOW__CORE__LOAD_EXAMPLES=False

# Set dags folder to airflow/dags
AIRFLOW__CORE__DAGS_FOLDER=/Users/harshbajwa/Code/voila/airflow/dags
